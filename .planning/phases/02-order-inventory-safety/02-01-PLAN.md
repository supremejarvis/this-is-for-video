---
phase: 02-order-inventory-safety
plan: 01
type: execute
wave: 3
depends_on: [01-01]
files_modified:
  - backend/app/api/v1/endpoints/orders.py
  - backend/app/services/inventory.py
  - backend/app/services/quote_service.py
  - backend/app/models/inventory.py
  - backend/app/models/guest_order_access.py
  - backend/app/services/guest_order_access.py
  - backend/app/api/deps.py
  - backend/alembic/versions/011_guest_order_access.py
  - backend/app/schemas/order.py
  - backend/tests/integration/test_inventory_concurrency.py
  - backend/tests/integration/test_order_transaction.py
  - backend/tests/integration/test_guest_order_ownership.py
autonomous: true
requirements: [ARCH-03, MONEY-05, PAY-04]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Final order creation revalidates quote expiry/version, current prices, and stock.
    - Reservation, order, and payment intent commit or roll back together.
    - PostgreSQL concurrent requests cannot oversell a SKU.
    - Guest order access uses a configurable expiry, hashed one-time-return token, order scope, ownership checks, revocation, rate limiting, replay protection, and payment authorization.
  artifacts:
    - backend/tests/integration/test_inventory_concurrency.py
    - backend/tests/integration/test_order_transaction.py
  key_links:
    - order endpoint -> locked inventory service -> transaction commit
    - preview quote -> final revalidation
---

<objective>Make final order creation a PostgreSQL-transactional, idempotent, ownership-checked operation.</objective>
<context>
@backend/app/api/v1/endpoints/orders.py
@backend/app/services/inventory.py
@backend/app/services/quote_service.py
@backend/app/models/inventory.py
@backend/app/models/order.py
@backend/tests/conftest.py
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Revalidate preview and commit order reservation atomically</name>
  <files>backend/app/api/v1/endpoints/orders.py, backend/app/services/quote_service.py, backend/app/services/inventory.py, backend/app/schemas/order.py, backend/tests/integration/test_order_transaction.py</files>
  <behavior>- Missing/expired/stale preview is rejected. - Current price or stock change returns a deterministic conflict. - Repeated idempotency key returns the same order. - Any reservation/order/payment-intent error rolls back the transaction.</behavior>
  <action>Require preview/quote identity and idempotency for final creation. Lock and re-read inventory and active price versions inside one transaction. Preserve guest checkout but add a cryptographically random raw token returned only once, storing only its hash with configurable expiry, order scope, revocation, ownership dependencies, and rate-limit/replay checks. Do not hardcode retention or expiry duration; read it from configuration pending business approval.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/integration/test_order_transaction.py tests/integration/test_guest_order_ownership.py -q</automated></verify>
  <done>Final order creation rejects stale intent and cannot partially commit order state.</done>
</task>
<task type="auto">
  <name>Task 2: Prove PostgreSQL concurrency and reservation lifecycle</name>
  <files>backend/tests/integration/test_inventory_concurrency.py, backend/tests/integration/test_reservation_release_audit.py, backend/tests/integration/conftest.py, docker-compose.test.yml</files>
  <action>Run inventory race, expiry/release audit, and rollback tests against the existing postgres_test service. Require the repository test environment to provide TEST_DATABASE_URL, or provide DATABASE_URL as the configured fallback; do not construct or print credentials in the plan or command. Fail clearly before starting tests when neither variable is present. Keep SQLite fixtures only for tests that do not inspect PostgreSQL locking or dialect-specific constraints. Add order idempotency unique-constraint coverage without changing protected files.</action>
  <verify><automated>if ([string]::IsNullOrWhiteSpace($env:TEST_DATABASE_URL) -and [string]::IsNullOrWhiteSpace($env:DATABASE_URL)) { throw 'TEST_DATABASE_URL or DATABASE_URL is required for PostgreSQL integration tests' }; docker compose -f docker-compose.test.yml up -d postgres_test; Push-Location backend; try { .\.venv\Scripts\python.exe -m pytest tests/integration/test_inventory_concurrency.py tests/integration/test_reservation_release_audit.py -q } finally { Pop-Location }</automated></verify>
  <done>Concurrent PostgreSQL tests demonstrate no overselling and audited reservation release.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| Concurrent clients -> PostgreSQL inventory rows | Competing reservations and repeated order requests. |
| Guest browser -> order access | Unauthenticated order access attempts. |
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Race condition | Inventory reservation | critical | mitigate | PostgreSQL row locks and integration tests. |
| T-02-02 | Spoofing | Guest order access | critical | mitigate | Hashed signed guest token, expiry, ownership checks. |
| T-02-SC | Tampering | Test database setup | high | mitigate | Disposable PostgreSQL and package legitimacy review. |
</threat_model>
<verification>Run PostgreSQL integration tests, targeted unit tests, and final order API contract checks.</verification>
<success_criteria>Order finalization is idempotent, ownership-aware, stale-safe, and concurrency-safe under PostgreSQL.</success_criteria>
<output>Create .planning/phases/02-order-inventory-safety/02-01-SUMMARY.md when executed.</output>
