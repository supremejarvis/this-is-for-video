---
phase: 00-protected-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Current dirty-file ownership is recorded without changing protected files.
    - Active runtime, migration, test, and integration baselines are reproducible.
  artifacts:
    - .planning/phases/00-protected-baseline/BASELINE.md
  key_links:
    - package scripts -> actual verification commands
    - Docker/Alembic configuration -> current database lifecycle
---

<objective>
Establish a read-only implementation baseline and ownership gate before any migration work.

Purpose: Prevent accidental overwrites and distinguish current evidence from stale mapping claims.
Output: Only `.planning/phases/00-protected-baseline/BASELINE.md`. Build/test tools may create generated artifacts such as `.next`, reports, coverage, and caches, but may not modify source files or protected files.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@next-env.d.ts
@package-lock.json
@backend/tests/unit/test_orders_and_payments.py
@package.json
@backend/pyproject.toml
@backend/docker-entrypoint.sh
@backend/app/main.py
@backend/alembic/env.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Capture protected ownership and runtime baseline</name>
  <files>.planning/phases/00-protected-baseline/BASELINE.md</files>
  <action>Record git status and scoped diffs for next-env.d.ts, package-lock.json, and backend/tests/unit/test_orders_and_payments.py. Record that these files are BLOCKED_PENDING_OWNERSHIP and must not be edited. Record active Next.js entrypoints, FastAPI entrypoint/router aggregation, current Alembic revision evidence, and whether a PostgreSQL URL is available. Do not change source or protected files.</action>
  <verify><automated>git status --short; git diff -- next-env.d.ts package-lock.json backend/tests/unit/test_orders_and_payments.py; npm run typecheck; npm run test:unit -- --run; npm run build; cd backend; .\.venv\Scripts\python.exe -m pytest --collect-only -q</automated></verify>
  <done>Baseline records exact dirty-file state, available scripts, active entrypoints, and all commands that completed or failed.</done>
</task>
<task type="auto">
  <name>Task 2: Inventory integration and fallback paths</name>
  <files>.planning/phases/00-protected-baseline/BASELINE.md</files>
  <action>Append evidence-backed inventories for direct external fetches, mock/runtime imports, localStorage/sessionStorage/in-memory persistence, duplicate /api/v1 paths, Alembic revisions, backend test fixtures, Playwright route mocks, and deployment references. Mark each inventory item as runtime, test-only, deployment-only, or unknown.</action>
  <verify><automated>Get-ChildItem src,backend,tests -Recurse -File | Select-String -Pattern 'fetch\\(|axios|/api/v1|mockData|localStorage|sessionStorage|create_all|DATABASE_URL'; Select-String -Path package.json,next.config.ts,vercel.json,docker-compose*.yml -Pattern 'fetch\\(|axios|/api/v1|mockData|localStorage|sessionStorage|create_all|DATABASE_URL'; Get-ChildItem backend/alembic/versions; Select-String -Path backend/app/**/*.py -Pattern 'revision|down_revision' -ErrorAction SilentlyContinue</automated></verify>
  <done>Every later phase has a grounded baseline and no cleanup task relies on filename or search count alone.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Worktree -> planning artifact | Dirty source state could be overwritten by planning activity. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-00-01 | Tampering | Protected files | critical | mitigate | Read-only diff capture and ownership gate. |
| T-00-SC | Tampering | Tooling commands | high | mitigate | No installs, edits, or destructive commands in baseline. |
</threat_model>

<verification>Baseline commands and inventories are captured; protected files remain byte-for-byte untouched.</verification>
<success_criteria>Phase 0 produces evidence sufficient to start Phase 1 without guessing ownership or runtime scope.</success_criteria>
<output>Create .planning/phases/00-protected-baseline/00-01-SUMMARY.md when executed.</output>
