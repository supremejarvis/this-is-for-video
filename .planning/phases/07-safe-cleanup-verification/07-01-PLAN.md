---
phase: 07-safe-cleanup-verification
plan: 01
type: execute
wave: 8
depends_on: [06-01]
files_modified:
  - scripts/verify-architecture-boundaries.mjs
autonomous: false
requirements: [ARCH-01, ARCH-02, ARCH-03, MONEY-05, PAY-04]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - No candidate is removed until replacement, data migration, import, build, test, integration, contract, and browser gates pass.
    - Runtime mock data and local authoritative persistence are absent after explicit review.
    - Direct browser private-provider calls are absent.
  artifacts:
    - scripts/verify-architecture-boundaries.mjs
  key_links:
    - static import/deployment scan -> deletion decision
    - replacement API -> migrated persisted data -> cleanup
---

<objective>Perform only gated cleanup after all approved replacements are live, preserving rollback and the protected-file boundary.</objective>
<context>
@.planning/phases/00-protected-baseline/BASELINE.md
@src/data/mockData.ts
@src/services/quoteService.ts
@src/services/order.service.ts
@src/services/catalogService.ts
@src/services/apiService.ts
@src/services/ceptIndiaPostService.ts
@src/app/api/inquiries/route.ts
@src/store/useStore.ts
</context>
<tasks>
<task type="auto">
  <name>Task 1: Add deletion-gate verification and deprecate candidates</name>
  <files>scripts/verify-architecture-boundaries.mjs, src/data/mockData.ts, src/services/quoteService.ts, src/services/order.service.ts, src/services/catalogService.ts, src/services/product.service.ts, src/services/apiService.ts, src/services/ceptIndiaPostService.ts, src/app/api/inquiries/route.ts, src/store/useStore.ts, backend/app/main.py</files>
  <action>Before deleting anything, add a non-destructive verification script that checks production imports, dynamic/lazy imports, tests/scripts, service-worker references, Docker/Vercel references, protected-file status, replacement-live evidence, persisted-data accounting, and rollback evidence. This task must not delete or modify any candidate. Preserve compatibility exports where rollback requires them. Never touch next-env.d.ts, package-lock.json, or backend/tests/unit/test_orders_and_payments.py.</action>
  <verify><automated>node scripts/verify-architecture-boundaries.mjs; npm run typecheck; npm run test:unit -- --run; npm run build</automated></verify>
  <done>Every cleanup action has machine-checkable evidence and no protected file changed.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve deletion review and produce exact deletion list</name>
  <files>All candidate files listed in Task 1</files>
  <action>Present the deletion-gate report for separate human review. Confirm persisted inquiry data migration, replacement API availability, rollback artifacts, PostgreSQL integration results, contract results, and relevant Playwright results. Do not proceed on an unapproved candidate or if any protected file ownership is unresolved.</action>
  <verify><automated>node scripts/verify-architecture-boundaries.mjs</automated><human-check>Reviewer confirms each candidate's prerequisite checklist, persisted-data accounting, rollback evidence, protected-file diff, and exact approved deletion list.</human-check></verify>
  <done>An exact approved deletion list exists; no candidate has been deleted or modified before this checkpoint.</done>
</task>
<task type="auto">
  <name>Task 3: Execute only the explicitly approved cleanup set</name>
  <files>Approved candidates only, as named by Task 2</files>
  <action>After Task 2 approval, remove only candidates on the exact approved deletion list. Preserve every unapproved compatibility file. Do not touch protected files.</action>
  <verify><automated>node scripts/verify-architecture-boundaries.mjs</automated></verify>
  <done>Only explicitly approved candidates are removed and unapproved compatibility paths remain intact.</done>
</task>
<task type="auto">
  <name>Task 4: Run complete post-cleanup verification</name>
  <files>None beyond approved cleanup set</files>
  <action>Run frontend, backend, PostgreSQL, contract, and real browser verification after approved cleanup. Report sandboxed provider limitations and do not infer production readiness from build/test success.</action>
  <verify><automated>npm run typecheck; npm run test:unit -- --run; npm run build; npm run test:e2e; if ([string]::IsNullOrWhiteSpace($env:TEST_DATABASE_URL) -and [string]::IsNullOrWhiteSpace($env:DATABASE_URL)) { throw 'TEST_DATABASE_URL or DATABASE_URL is required for PostgreSQL integration tests' }; docker compose -f docker-compose.test.yml up -d postgres_test; Push-Location backend; try { .\.venv\Scripts\python.exe -m pytest -q } finally { Pop-Location }</automated></verify>
  <done>All required verification classes have been run and results are recorded without a production-readiness claim.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| Cleanup -> runtime | Removing a hidden consumer can break production behavior. |
| Migration -> persisted data | Deleting a compatibility path can strand existing records. |
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | Cleanup candidates | critical | mitigate | Static/runtime/deployment scans and human deletion review. |
| T-07-02 | Availability | Persisted inquiry/order data | critical | mitigate | Data migration and rollback verification before removal. |
| T-07-SC | Tampering | Cleanup tooling | high | mitigate | No package installs; protected files remain read-only. |
</threat_model>
<verification>Run deletion-gate scan, frontend/backend suites, PostgreSQL integration, contracts, and relevant Playwright flows after approved cleanup only.</verification>
<success_criteria>Legacy code is removed only where replacement and rollback evidence exists; no production readiness claim is made from build/test results alone.</success_criteria>
<output>Create .planning/phases/07-safe-cleanup-verification/07-01-SUMMARY.md when executed.</output>
