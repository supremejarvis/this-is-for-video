# --------------------------------------------------------------------------------------------------
# Apollo Engineering — OWASP ZAP Staging Baseline Scan Configuration
# STAGING ONLY: NEVER EXECUTE ACTIVE SCANS AGAINST PRODUCTION DOMAINS
# --------------------------------------------------------------------------------------------------
# Rule configuration format:
# <rule-id>\t<IGNORE|WARN|FAIL>\t<reason>
# --------------------------------------------------------------------------------------------------

# Content Security Policy (CSP) Header Not Set
10038	WARN	(Evaluated in staging environment)

# Strict-Transport-Security Header Not Set (handled by CDN/Reverse Proxy in production)
10035	WARN	(Staging HTTP preview without SSL termination)

# X-Content-Type-Options Header Missing
10021	FAIL	(Must be configured on all static and dynamic responses)

# Anti-clickjacking Header (X-Frame-Options)
10020	FAIL	(Must prevent clickjacking on checkout and payment pages)

# Server Leaks Information via "Server" HTTP Response Header Field
10036	WARN	(Mask server headers in production)

# Timestamp - Unix
10096	IGNORE	(Expected in public API time responses)

# Cross-Domain JavaScript Source File Inclusion
10017	WARN	(Legitimate CDNs: Google Fonts, MSG91, HostNSoft)
