---
phase: 07-safe-cleanup-verification
plan: 03
type: execute
wave: 9
depends_on: [07-01]
files_modified:
  - playwright.real.config.ts
  - package.json
  - tests/e2e/real/buyer_checkout_backend.spec.ts
  - tests/e2e/real/admin_catalog_backend.spec.ts
  - tests/e2e/real/inquiry_backend.spec.ts
  - tests/e2e/real/provider_boundary.spec.ts
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, MONEY-05, PAY-01, PAY-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Real browser tests are separate from route-mocked UI tests.
    - The real browser environment connects Next.js to FastAPI and disposable PostgreSQL.
    - Browser network assertions prove private provider URLs are not called directly.
  artifacts:
    - playwright.real.config.ts
    - tests/e2e/real/buyer_checkout_backend.spec.ts
    - tests/e2e/real/provider_boundary.spec.ts
  key_links:
    - Playwright -> Next.js rewrite -> FastAPI -> PostgreSQL
    - browser network -> provider boundary assertion
---

<objective>Define a separate real-integration Playwright milestone so browser evidence is not confused with route-mocked UI tests.</objective>

<context>
@playwright.config.ts
@next.config.ts
@docker-compose.test.yml
@package.json
@tests/e2e/buyer_catalog_quote.spec.ts
@tests/e2e/admin_audit.spec.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add real integration Playwright environment</name>
  <files>playwright.real.config.ts, package.json, docker-compose.test.yml</files>
  <action>Create a separate Playwright config and package script that starts or connects to Next.js and FastAPI while docker-compose.test.yml provides disposable PostgreSQL. Define FASTAPI_BACKEND_URL, TEST_DATABASE_URL, migration/seed preconditions, isolated test data, teardown behavior, and optional Redis only if the exercised backend path requires it. Keep the existing mocked Playwright config and scripts unchanged. Do not place credentials in planning or source; use test-environment variables.</action>
  <verify><automated>npm run test:e2e:real -- --list</automated></verify>
  <done>A distinct real-integration command exists and its web/database prerequisites are explicit.</done>
</task>
<task type="auto">
  <name>Task 2: Add real backend-connected browser flows</name>
  <files>tests/e2e/real/buyer_checkout_backend.spec.ts, tests/e2e/real/admin_catalog_backend.spec.ts, tests/e2e/real/inquiry_backend.spec.ts, tests/e2e/real/provider_boundary.spec.ts</files>
  <action>Cover buyer catalog and preview, admin mutation followed by buyer refresh, inquiry persistence, checkout tampering/stale behavior, and guest isolation through the real Next-to-FastAPI path. Assert browser requests never target CEPT, India Post, MSG91, or other private provider URLs directly. Label any provider sandbox stubs explicitly and do not claim live-provider verification.</action>
  <verify><automated>npm run test:e2e:real -- tests/e2e/real/buyer_checkout_backend.spec.ts tests/e2e/real/admin_catalog_backend.spec.ts tests/e2e/real/inquiry_backend.spec.ts tests/e2e/real/provider_boundary.spec.ts</automated></verify>
  <done>Real browser evidence is separate, reproducible, and demonstrably exercises the backend boundary.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Browser -> Next -> FastAPI | Cookies, rewrites, CSRF, and backend authority. |
| Browser -> external providers | Private provider URLs and secrets must not cross into browser runtime. |
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-05 | Information disclosure | Browser provider calls | critical | mitigate | Network assertions and source-boundary scans. |
| T-07-06 | Spoofing | Real guest checkout | critical | mitigate | Backend ownership and PostgreSQL test data. |
| T-07-SC | Tampering | Test environment | high | mitigate | Existing dependencies only; no package installation. |
</threat_model>

<verification>Run the real integration command only after PostgreSQL, FastAPI, Next.js, migrations, and seed data are available.</verification>
<success_criteria>Mocked UI tests and real backend-connected browser tests are clearly separated and independently reportable.</success_criteria>
<output>Create .planning/phases/07-safe-cleanup-verification/07-03-SUMMARY.md when executed.</output>
