---
phase: 01-checkout-preview-authority
plan: 01
type: execute
wave: 2
depends_on: [00-01]
files_modified:
  - backend/app/api/v1/endpoints/orders.py
  - backend/app/schemas/order.py
  - backend/app/schemas/quote.py
  - backend/app/services/quote_service.py
  - backend/app/services/shipping_service.py
  - backend/tests/contract/test_orders_preview_contract.py
  - backend/tests/unit/test_pricing.py
autonomous: true
requirements: [MONEY-01, MONEY-02, MONEY-03, MONEY-04, MONEY-05, MONEY-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Preview accepts customer intent only and derives every monetary value from backend state.
    - Client-provided base_shipping, prices, taxes, fees, and totals cannot alter preview output.
    - Preview is read-only and does not reserve stock.
    - Discounts are unsupported unless a verified backend discount rule exists; no client discount calculation is introduced.
  artifacts:
    - backend/app/api/v1/endpoints/orders.py
    - backend/tests/contract/test_orders_preview_contract.py
  key_links:
    - preview router -> quote service -> price/product/shipping services
    - preview response -> canonical frontend quote client
---

<objective>Implement the real POST /api/v1/orders/preview contract and move checkout display authority to it without deleting the old quote route.</objective>

<context>
@backend/app/api/v1/endpoints/quotes.py
@backend/app/services/quote_service.py
@backend/app/schemas/quote.py
@backend/app/schemas/order.py
@src/services/api/quoteApi.ts
@src/components/checkout/CheckoutModal.tsx
@src/components/checkout/CartDrawer.tsx
@src/store/useStore.ts
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Add preview endpoint and authoritative contract</name>
  <files>backend/app/api/v1/endpoints/orders.py, backend/app/schemas/order.py, backend/app/schemas/quote.py, backend/app/services/quote_service.py, backend/app/services/shipping_service.py, backend/tests/contract/test_orders_preview_contract.py</files>
  <behavior>- Valid prepaid and COD intent returns item prices, GST, no discount unless a verified backend rule exists, shipping, shipping GST, COD charge, rounding, totals, stock status, quote ID/version, calculation version, and expiry. - Client unit_price, GST, base_shipping, COD charge, grand total, and unauthorized extra financial fields cannot alter the response and are rejected where the schema forbids them. - Preview creates no order, payment, or inventory reservation. - Invalid SKU, quantity, unavailable stock, stale price/version, and shipping failure return normalized errors.</behavior>
  <action>Introduce a dedicated preview request/response contract under the orders domain. Reuse Decimal pricing and catalog resolution only after removing client shipping authority. Discounts are explicitly unsupported unless a verified backend discount rule is added later; do not invent discount calculations. Report stock availability without reservation. Preserve /quotes during a compatibility window and make preview the only new checkout contract.</action>
  <verify><automated>cd backend; .\.venv\Scripts\python.exe -m pytest tests/contract/test_orders_preview_contract.py tests/unit/test_pricing.py -q</automated></verify>
  <done>Preview contract is implemented and covered by backend contract tests without touching protected test_orders_and_payments.py.</done>
</task>
<task type="auto">
  <name>Task 2: Connect checkout display to preview only</name>
  <files>src/services/api/quoteApi.ts, src/services/quoteService.ts, src/store/useStore.ts, src/components/checkout/CheckoutModal.tsx, src/components/checkout/CartDrawer.tsx, src/services/api/client.ts</files>
  <action> Add a canonical preview method and normalize API paths through the existing client. Switch CheckoutModal, CartDrawer, and store quote state to render only unexpired server preview totals. On API failure show an error/loading state; remove fallback financial calculations from this checkout path. Do not edit next-env.d.ts, package-lock.json, or backend/tests/unit/test_orders_and_payments.py.</action>
  <verify><automated>npm run typecheck; npm run test:unit -- --run src/components/checkout/__tests__/CartDrawerCheckout.test.tsx src/components/checkout/__tests__/CheckoutFormRequirements.test.tsx; npm run build</automated></verify>
  <done>Checkout UI has no fallback total authority and all canonical preview requests use one normalized API base path.</done>
</task>
</tasks>

<threat_model>
| Boundary | Description |
|---|---|
| Browser -> preview API | Untrusted quantities, identifiers, addresses, and attempted monetary overrides. |

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Tampering | Preview request | critical | mitigate | Ignore/reject client monetary fields and derive from DB. |
| T-01-02 | Elevation | Checkout UI | high | mitigate | Render server preview only. |
| T-01-SC | Tampering | Dependencies | high | mitigate | No package changes in this phase. |
</threat_model>

<verification>Run targeted backend contract tests, frontend checkout tests, typecheck, and build.</verification>
<success_criteria>Preview is authoritative, read-only, complete, and visibly drives checkout totals.</success_criteria>
<output>Create .planning/phases/01-checkout-preview-authority/01-01-SUMMARY.md when executed.</output>
