---
phase: 04-inquiries-api-consolidation
plan: 01
type: execute
wave: 5
depends_on: [03-01]
files_modified:
  - backend/app/models/inquiry.py
  - backend/app/schemas/inquiry.py
  - backend/app/services/inquiry_service.py
  - backend/app/api/v1/endpoints/inquiries.py
  - backend/app/api/v1/api.py
  - backend/app/cli/import_inquiries.py
  - backend/alembic/versions/012_inquiries.py
  - src/services/api/inquiryApi.ts
  - src/services/api/index.ts
  - src/app/api/inquiries/route.ts
  - backend/tests/contract/test_inquiry_api.py
  - backend/tests/integration/test_inquiry_import.py (planned new file)
autonomous: true
requirements: [ARCH-02, ARCH-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Inquiries persist in PostgreSQL through FastAPI.
    - Existing JSON inquiries are migrated before compatibility route removal.
    - Browser calls use the canonical client and normalized paths.
  artifacts:
    - backend/alembic/versions/012_inquiries.py
    - src/services/api/inquiryApi.ts
  key_links:
    - contact UI -> inquiryApi -> FastAPI -> PostgreSQL
    - existing inquiries.json -> import command/service -> inquiry table
---

<objective>Move inquiry persistence to PostgreSQL and consolidate browser API wrappers without deleting compatibility code prematurely.</objective>
<context>
@src/app/api/inquiries/route.ts
@src/services/apiService.ts
@src/services/api/client.ts
@src/services/api/index.ts
@backend/app/api/v1/api.py
@.planning/inquiries.json
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Implement inquiry domain and migration</name>
  <files>backend/app/models/inquiry.py, backend/app/schemas/inquiry.py, backend/app/services/inquiry_service.py, backend/app/api/v1/endpoints/inquiries.py, backend/app/api/v1/api.py, backend/alembic/versions/012_inquiries.py, backend/tests/contract/test_inquiry_api.py</files>
  <behavior>- Valid inquiry creates one PostgreSQL record idempotently. - Invalid contact data returns normalized errors. - Admin access is RBAC-protected. - Existing records can be imported without duplicates.</behavior>
  <action>Create the inquiry model, schema, domain service, router, migration, and import command for .planning/inquiries.json. The command must support --dry-run and explicit --commit modes, duplicate detection, idempotent reruns, imported/skipped/rejected counts, structured error reporting, source checksum or backup evidence, and transaction-safe failure with no uncontrolled partial import. Do not modify or delete the existing JSON route in this task.</action>
  <verify><automated>Push-Location backend; try { .\.venv\Scripts\python.exe -m app.cli.import_inquiries --source ..\.planning\inquiries.json --dry-run; .\.venv\Scripts\python.exe -m pytest tests/contract/test_inquiry_api.py -q; .\.venv\Scripts\python.exe -m pytest tests/integration/test_inquiry_import.py -q } finally { Pop-Location }</automated></verify>
  <done>The import command exposes dry-run and explicit commit paths, reports imported/skipped/rejected records and structured failures, preserves checksum/backup evidence, commits transactionally, and supports an idempotent rerun without duplicate rows.</done>
</task>
<task type="auto">
  <name>Task 2: Consolidate frontend API wrappers and compatibility route</name>
  <files>src/services/api/inquiryApi.ts, src/services/api/index.ts, src/services/apiService.ts, src/services/catalogService.ts, src/services/product.service.ts, src/services/quoteService.ts, src/services/order.service.ts, src/services/api/adminPaymentApi.ts, src/services/api/adminShippingApi.ts, src/services/api/adminReturnsApi.ts, src/app/api/inquiries/route.ts</files>
  <action>Add canonical inquiry client and migrate runtime callers. Convert legacy wrappers to delegating compatibility facades, normalize all endpoint paths relative to the shared client, and change the Next inquiry route into a temporary proxy/read-only compatibility path that is not the authoritative JSON writer. Add an executable accounting gate that runs dry-run, proves zero rows were committed, runs explicit --commit, records imported/skipped/rejected counts, reruns the same commit to prove no duplicates, verifies failures are surfaced without silent success, and blocks route removal until every source record is imported, rejected with a reason, or explicitly skipped. Do not delete wrappers or the route until Phase 7 gates pass.</action>
  <verify><automated>npm run typecheck; npm run test:unit -- --run src/services/__tests__/apiService.test.ts; npm run build</automated></verify>
  <done>Runtime callers use canonical clients and no client path contains a duplicated /api/v1 prefix.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| Browser -> inquiry API | Untrusted PII and duplicate submissions. |
| Compatibility route -> backend | Dual-write/dual-read migration risk. |
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | Inquiry payload | high | mitigate | Pydantic validation, RBAC, idempotency, audit. |
| T-04-02 | Information disclosure | Inquiry admin access | high | mitigate | Role-protected reads and field filtering. |
| T-04-SC | Tampering | New packages | high | mitigate | No new packages planned. |
</threat_model>
<verification>Run inquiry contract tests, frontend typecheck/unit/build, and inspect compatibility consumers.</verification>
<success_criteria>Inquiry business state is database-backed and browser API behavior is consolidated without premature deletion.</success_criteria>
<output>Create .planning/phases/04-inquiries-api-consolidation/04-01-SUMMARY.md when executed.</output>
