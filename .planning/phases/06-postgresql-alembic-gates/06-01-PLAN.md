---
phase: 06-postgresql-alembic-gates
plan: 01
type: execute
wave: 7
depends_on: [05-01]
files_modified:
  - backend/app/main.py
  - backend/app/core/database.py
  - backend/app/core/config.py
  - backend/docker-entrypoint.sh
  - backend/tests/integration/test_migrations.py
  - backend/tests/integration/conftest.py
  - docker-compose.test.yml
  - .github/workflows/backend.yml
  - backend/tests/unit/test_configuration.py
  - .env.test.example (planned non-secret test-environment template if URL injection is not already available)
autonomous: true
requirements: [ARCH-02, ARCH-03]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Staging/production require PostgreSQL and Alembic.
    - Migration failure stops deployment.
    - Production startup never calls create_all.
    - SQLite is limited to isolated non-PostgreSQL unit tests.
    - Redis failure cannot alter business correctness.
  artifacts:
    - backend/tests/integration/test_migrations.py
    - .github/workflows/backend.yml
  key_links:
    - deployment entrypoint -> alembic upgrade head -> FastAPI readiness
    - test compose -> PostgreSQL integration suite
---

<objective>Make PostgreSQL/Alembic deployment behavior explicit and testable without touching the protected package lock or test file.</objective>
<context>
@backend/app/main.py
@backend/app/core/database.py
@backend/app/core/config.py
@backend/docker-entrypoint.sh
@backend/alembic/env.py
@docker-compose.test.yml
@backend/tests/conftest.py
@package.json
@backend/pyproject.toml
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Enforce migration-managed production startup</name>
  <files>backend/app/main.py, backend/app/core/database.py, backend/app/core/config.py, backend/docker-entrypoint.sh, backend/tests/unit/test_configuration.py, backend/tests/integration/test_migrations.py</files>
  <behavior>- Production/staging rejects SQLite and placeholder secrets. - Startup does not create schema. - Alembic failure prevents application startup. - Current migration chain upgrades clean PostgreSQL database.</behavior>
  <action>Separate test schema creation from application startup. Make deployment migration failure fatal. Preserve Alembic revision files and add configuration/startup assertions. Do not alter the protected unit test file; add independent tests for migration gates.</action>
  <verify><automated>cd backend; .venv\Scripts\python -m pytest tests/unit/test_configuration.py tests/integration/test_migrations.py -q</automated></verify>
  <done>Production lifecycle is migration-controlled and fails closed on invalid database/configuration state.</done>
</task>
<task type="auto">
  <name>Task 2: Add disposable PostgreSQL CI gate</name>
  <files>docker-compose.test.yml, backend/tests/integration/conftest.py, .github/workflows/backend.yml</files>
  <action>Use the existing postgres_test service from docker-compose.test.yml and its repository-defined environment names POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, and POSTGRES_HOST_AUTH_METHOD. Require TEST_DATABASE_URL, or DATABASE_URL as the configured fallback, to be supplied by the test environment; never derive, invent, or print a password in the command or report. Fail clearly before tests when neither URL variable is present. If the repository lacks a safe complete URL injection mechanism, create the planned non-secret .env.test.example template with variable names only and no values. SQLite remains limited to isolated unit tests. Cover current-revision-to-head and fresh-database-to-head upgrades, PostgreSQL row locking, rollback or documented forward-fix strategy, create_all absence in staging/production, and fatal Alembic failure. Add only the smallest CI command/configuration needed; do not invent npm scripts.</action>
  <verify><automated>if ([string]::IsNullOrWhiteSpace($env:TEST_DATABASE_URL) -and [string]::IsNullOrWhiteSpace($env:DATABASE_URL)) { throw 'TEST_DATABASE_URL or DATABASE_URL is required for PostgreSQL integration tests' }; docker compose -f docker-compose.test.yml up -d postgres_test; Push-Location backend; try { .\.venv\Scripts\python.exe -m pytest tests/integration/test_migrations.py tests/integration/test_inventory_concurrency.py -q } finally { Pop-Location }</automated></verify>
  <done>CI has a repeatable PostgreSQL integration gate distinct from SQLite unit tests.</done>
</task>
</tasks>
<threat_model>
| Boundary | Description |
|---|---|
| Deployment -> database schema | Incorrect or partial migration can corrupt startup behavior. |
| Test environment -> production assumptions | SQLite can falsely validate PostgreSQL behavior. |
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Migration chain | critical | mitigate | Preserve revisions, test upgrade, stop on failure. |
| T-06-02 | Availability | Startup dependency | high | mitigate | Explicit readiness and fatal migration handling. |
| T-06-SC | Tampering | CI dependencies | high | mitigate | Existing Docker/package dependencies only. |
</threat_model>
<verification>Run configuration, migration, PostgreSQL integration, and CI-equivalent backend commands.</verification>
<success_criteria>Staging/production schema behavior is controlled solely by Alembic against PostgreSQL.</success_criteria>
<output>Create .planning/phases/06-postgresql-alembic-gates/06-01-SUMMARY.md when executed.</output>
